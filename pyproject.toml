[tool.poetry]
name = "quarto-test"
version = "0.1.0"
description = ""
authors = []
readme = ""
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
jupyter = "^1.0.0"
pyyaml = "^6.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
